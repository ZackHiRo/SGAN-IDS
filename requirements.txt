# StealthGAN-IDS requirements
# For both CPU and GPU (CUDA 11.8+ for RTX 4090)
torch>=2.0.0
# If using GPU, install the correct CUDA version, e.g.:
# pip install torch==2.1.0+cu118 torchvision==0.16.0+cu118 -f https://download.pytorch.org/whl/torch_stable.html
torchvision>=0.15.0
numpy>=1.21
pandas>=1.3
scikit-learn>=1.0
matplotlib>=3.4
mlflow>=2.0
joblib>=1.1
# For tqdm progress bars (optional, but nice)
tqdm>=4.60
# For argument parsing (standard library, but include for completeness)
argparse
# For experiment tracking (optional, if using Weights & Biases)
wandb>=0.15 